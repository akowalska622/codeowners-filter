{
  "name": "codeowners-filter",
  "displayName": "CODEOWNERS Filter",
  "description": "Filters files in the tree view based on the CODEOWNERS file.",
  "version": "0.0.1",
  "publisher": "Ania Kowalska",
  "engines": {
    "vscode": "^1.97.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "codeOwners.refresh",
        "title": "Refresh CODEOWNERS View"
      },
      {
        "command": "codeOwners.selectOwner",
        "title": "Select Code Owner"
      }
    ],
    "views": {
      "explorer": [
        {
          "id": "codeOwnersView",
          "name": "CODEOWNERS View"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "dependencies": {
    "minimatch": "^5.0.0"
  },
  "devDependencies": {
    "@types/minimatch": "^5.1.2",
    "@types/mocha": "^10.0.10",
    "@types/node": "20.x",
    "@types/vscode": "^1.97.0",
    "@typescript-eslint/eslint-plugin": "^8.22.0",
    "@typescript-eslint/parser": "^8.22.0",
    "@vscode/test-cli": "^0.0.10",
    "@vscode/test-electron": "^2.4.1",
    "eslint": "^9.19.0",
    "typescript": "^5.7.3"
  }
}
